import { efficiencyMappings } from './efficiency-mappings';

describe('efficiencyMappings', () => {
    it('should work', () => {
        expect(efficiencyMappings()).toEqual('efficiency-mappings');
    });
});
